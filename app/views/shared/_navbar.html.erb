<% store = current_store %>
<% return unless store %>
<% cart = Cart.find_or_create_by(store: store) %>
  <% if store.theme == "topbar" %>
    <nav>
        <%= link_to "Home", store_path(store) %> |
        <%= link_to "About Us", about_store_path(store) %> |
        <details style="display:inline-block;">
          <summary>Products</summary>
          <% store.categories.each do |category| %>
            <div>
              <%= link_to category.name, store_path(store, anchor: category.name.parameterize ) %>
                <%= category.name %>
              
            </div>
          <% end %>
        </details> |

        <%= link_to "Cart", cart_path(cart) if cart %> |
        <%= link_to "My Orders", orders_path %> |
        <%= link_to "Contact Us", contact_store_path(store) %> |
        <%= link_to "FAQ", faq_store_path(store) %>
    </nav>

  <% elsif store.theme == "grid" %>
    <div>
      
      <button onclick="location.href='<%= store_path(store) %>'">Home</button>
      <button onclick="location.href='<%= about_store_path(store) %>'">About Us</button>
     

      <details style="display:inline-block;">
        <summary>Product</summary>
        <% store.categories.each do |category| %>
          <div>
            <%= link_to category.name, store_path(store, anchor: category.name.parameterize ) %>
              <%= category.name %>
            
          </div>
        <% end %>
      </details>

      <button onclick="location.href='<%= cart_path(cart) %>'">Cart</button>
      <button onclick="location.href='<%= orders_path %>'">My Orders</button>
      <button onclick="location.href='<%= contact_store_path(store) %>'">Contact us</button>
      <button onclick="location.href='<%= faq_store_path(store) %>'">FAQ</button>
      
    </div>

  <% else %>
    <details>
      <summary>Menu</summary>
      <ul>
        <li><%= link_to "Home", store_path(store) %></li>
        <li><%= link_to "About Us", about_store_path(store) %></li>

        <li>
          <details>
            <summary>Product</summary>
            <% store.categories.each do |category| %>
              <div>
                <%= link_to category.name, store_path(store, anchor: category.name.parameterize ) %>
                  <%= category.name %>
                
              </div>
            <% end %>
          </details>
        </li>

        <li><%= link_to "Cart", cart_path(cart) if cart %></li>
        <li><%= link_to "My Orders", orders_path %></li>
        <li><%= link_to "Contact Us", contact_store_path(store) %></li>
        <li><%= link_to "FAQ", faq_store_path(store) %></li>
        
      </ul>
    </details>
  <% end %>

